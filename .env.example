# Gemini API Key (Server-side AI Multimodal OCR)
GEMINI_API_KEY=

# MongoDB Connection String (Optional, in-memory fallback enabled if omitted)
MONGO_URI=

# JWT Secret
JWT_SECRET=ElectricityAnalyser

# SMTP Email Configuration for Password Reset OTP (Optional, in-app OTP fallback enabled if omitted)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
